// The global variable
var fixedValue = 4;

function incrementer () {
let newValue = (fixedValue+1) 
return newValue;
// In this function, we do not change the "fixedValue"
// instead, we assign a variable which depends on the fixedValue
}